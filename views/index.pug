doctype 
html 
    head 
        meta(charset='utf-8')
        title GIF ChatRoom 
    body
        div press F12 to check console tab and network tab
        script(src="/socket.io/socket.io.js")
        script.
            var socket = io.connect('http://localhost:3000', {
                path: '/socket.io'
            });

            socket.on('news', function (data) {
                console.log(data);
                socket.emit('reply', 'hi server')
            });